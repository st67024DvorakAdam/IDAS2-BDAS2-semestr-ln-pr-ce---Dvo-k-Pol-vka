<Window x:Class="Database_Hospital_Application.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Database_Hospital_Application"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        xmlns:vm="clr-namespace:Database_Hospital_Application.ViewModels">

    <Grid Margin="0,0,0,100" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Přihlašení" FontSize="21" HorizontalAlignment="Center" Grid.Row="0" Margin="0,0,0,20"/>

            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Username" VerticalAlignment="Center" FontSize="14"/>
                <TextBox Grid.Column="1" MaxWidth="500" Margin="6,0,0,0" Padding="2" Height="25"/>
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Content="Password" VerticalAlignment="Center" FontSize="14"/>
                <PasswordBox Grid.Column="1" MaxWidth="100" Margin="10,0,0,0" Padding="2" Height="25"/>
            </Grid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Přihlásit" Margin="0,0,10,0" Padding="10" FontSize="14"/>
                <Button Content="Pokračovat bez příhlášení" Padding="10" FontSize="14"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
